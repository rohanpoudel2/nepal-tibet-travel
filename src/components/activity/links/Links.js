import styles from "./links.module.scss";
import Options from "@/components/country/options/Options";

const Links = ({ data }) => (
  <Options data={data} title="Useful Links" />
);

export default Links